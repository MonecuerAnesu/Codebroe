specVersion: "v1"
serviceId: "codebroe"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "codebroe-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
